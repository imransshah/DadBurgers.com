{
  "name": "decolonized-chat-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Server-side chat proxy for decolonized — keeps the Anthropic API key off the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
